# winTerm v0.5 acceptance

## Verdict

Last updated: 2026-07-17.

**Not accepted.** Visual Docking is under development. The technical spike establishes that live tab and pane transfer is viable only between windows in the current winTerm process. Cross-process transfer is not supported, and no shell restart will be used as a fallback.

`[x]` means the exact source or script check was completed in this checkout. `[ ]` means unavailable, not implemented end to end, or not manually tested.

## Baseline

| Item | Recorded value |
| --- | --- |
| Branch before v0.5 | `main` |
| v0.5 branch | `codex/winterm-v0.5-visual-docking` |
| v0.4 baseline | `9890c1c0419998b1c59c946810a4cb7e12a6787f` |
| Microsoft Terminal baseline | `release-1.25@1cea42d433253d95c4487a3037db48197b5e72f4` |
| Package identity | `Kaname.winTerm`; alias `winterm.exe` |
| Application version at start | `0.4.0-dev` |
| Current application/package version | `0.5.0-alpha` / `0.5.0.0` |
| Workspace schema at start | `1` |
| Remotes | `origin` exists; `upstream` is absent |
| Initial uncommitted changes | None observed |

## Phase checkpoints

- [x] Inspected the v0.4 baseline, package identity, remotes, acceptance documents, Workspace model, persistence boundary, upstream tab tear-out, pane movement, process model, and terminal content ownership.
- [x] Recorded the ownership graph, process model, transfer constraints, chosen architecture, risks, and live-transfer decision in `visual-docking-spike.md`.
- [x] Confirmed same-process windows share one `ContentManager`.
- [x] Confirmed `ControlCore` retains the terminal buffer, renderer state, and `ITerminalConnection` while a `TermControl` is detached.
- [x] Confirmed inherited tab and pane drag payloads reject a different PID.
- [x] Implement Layout Model v2, stable Empty Slot IDs, and deterministic Workspace v1-to-v2 migration.
- [x] Implement deterministic edge/corner transformation, normalization, geometry, and validation source.
- [x] Source-check v2 valid, empty-slot, legacy, invalid, and security fixtures.
- [ ] Compile and run the new Layout Model and transformation unit tests.
- [x] Implement transaction ordering, rollback/recovery callbacks, session ownership leases, opaque expiring drag tokens, drag state transitions, and in-memory history source.
- [x] Source-check transformation, transaction, session-transfer, and history boundaries with dedicated scripts.
- [ ] Compile and run transaction, rollback, ownership, token, state-machine, and history unit tests.
- [x] Implement source-level target priority and hysteresis, overlay presentation, proposed-layout preview, keyboard navigation, accessible labels, and docking settings defaults.
- [x] Keep the runtime docking adapter disabled until compiled rollback and multi-window UI verification are available.
- [ ] Connect and verify the XAML drag/drop adapter, visual overlay, command palette, keybindings, and focus handling.
- [x] Preserve schema-v2 corner and empty-slot layouts through serialization and restore planning.
- [x] Add bounded redacted docking traces, layout health inspection, read-only layout validation, a local UI-suite manifest, and a non-runtime benchmark harness.
- [x] Add Docking source validation and Debug/Release compiled-test coverage to CI definitions.
- [ ] Connect and verify current-runtime layout diagnostics and the Settings diagnostics page.
- [ ] Complete compiled, UI automation, manual, package, DPI, and accessibility acceptance.

## Baseline checks actually run

| Check | Result |
| --- | --- |
| `test.ps1 -Suite Smoke -Configuration Debug -Platform x64` | Passed |
| `build.ps1 -Configuration Debug -Platform x64` | Unavailable: PowerShell 7 and the Visual Studio/MSBuild toolchain are not installed |
| `build.ps1 -Configuration Release -Platform x64` | Unavailable: PowerShell 7 and the Visual Studio/MSBuild toolchain are not installed |
| `validate-layout.ps1` Empty Slot / corner / invalid duplicate / normalize paths | Passed |
| Docking target, overlay, keyboard, Settings, and diagnostics source boundaries | Passed; compiled tests unavailable |
| Local docking UI suite manifest and privacy validation | Passed; runtime execution skipped |
| Docking PowerShell model benchmark | Passed; not a runtime UI or live-transfer measurement |
| Compiled unit tests | Not run; no compiler or test host |
| Package build and launch | Not run; no build output or package |

## Live-transfer decision

| Capability | Status |
| --- | --- |
| Same-window pane transfer primitive | Supported by inherited object movement; v0.5 transaction pending |
| Same-process cross-window tab transfer primitive | Supported by inherited content detach/attach; v0.5 transaction pending |
| Same-process cross-window pane transfer primitive | Supported by inherited content detach/attach; v0.5 transaction pending |
| Cross-process tab transfer | Not supported |
| Cross-process pane transfer | Not supported |

Do not change this verdict or mark runtime criteria complete without actual build, test, package, launch, accessibility, multi-window, mixed-DPI, and manual evidence.
