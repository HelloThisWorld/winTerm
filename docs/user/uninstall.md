# Uninstall winTerm

Uninstall winTerm through Windows Apps settings. This removes the application package but follows MSIX platform behavior for local app data. Delete only winTerm-specific local data if you choose a full reset; never remove Windows Terminal data, PowerShell profiles, WSL distributions, shell history, or unrelated user folders.
