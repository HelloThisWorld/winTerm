# Install winTerm

Download only the official release MSIX and its `SHA256SUMS.txt`. Verify the hash before installation. The current development package is unsigned; do not treat it as a signed public release. winTerm uses its own `Kaname.winTerm` identity and `winterm.exe` alias and does not replace Windows Terminal or `wt.exe`.
