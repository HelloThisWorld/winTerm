# Diagnostics

Create a diagnostic bundle only when requested and review it before sharing. A safe bundle excludes commands, terminal output, clipboard content, raw Workspaces, SSH configuration, environment variables, and full paths by default. Redaction is a release gate, not a substitute for review.
