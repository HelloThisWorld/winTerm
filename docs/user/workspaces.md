# Workspaces

Save and restore named Workspaces from the Workspace UI. Imports are data-only and reject embedded commands and executables. If restore cannot use a saved monitor or folder, winTerm uses a safe fallback instead of executing a path.
