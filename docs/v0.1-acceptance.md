# winTerm v0.1 acceptance

## Status

Last updated: 2026-07-13.

`[x]` means the stated check was actually completed. `[ ]` means blocked, skipped, or still requires manual verification. A source inspection is not treated as a runtime pass.

**Release verdict: not yet accepted.** The source-level foundation and Smoke suite pass, but this implementation environment lacks PowerShell 7, Visual Studio/MSBuild, and Windows SDK 10.0.22621.0. No executable or MSIX was produced, and the runtime checklist remains open.

## Repository and scope

- [x] Official Microsoft Terminal stable source used; no third-party fork used.
- [x] Baseline recorded as `release-1.25` commit `1cea42d433253d95c4487a3037db48197b5e72f4`.
- [x] `origin` and official `upstream` remotes configured.
- [x] WinTerm changes are separated by brand condition and owned directories.
- [x] Microsoft MIT license, Microsoft copyright, third-party `NOTICE.md`, and package `NOTICE.html` remain present.
- [x] No certificate, private key, publisher credential, third-party theme, bundled font, or downloaded logo added.
- [x] No post-v0.1 feature from the prohibited scope was implemented.
- [x] Protected terminal engine behavior was not rewritten.

## Automated source validation

- [x] All PowerShell files in `scripts/winterm` parse successfully under Windows PowerShell 5.1.
- [x] `test.ps1 -Suite Smoke` passes.
- [x] Source manifest parses as XML.
- [x] Package display name and application display name are `winTerm`.
- [x] Package identity is `HelloThisWorld.winTerm`, not a Microsoft Terminal identity.
- [x] Package registers `winterm.exe` and does not register `wt.exe`.
- [x] Description states independent open-source status without claiming Microsoft endorsement.
- [x] Original winTerm SVG, PNG, and ICO resources exist at required package sizes.
- [x] Unpackaged state root is `%LOCALAPPDATA%\winTerm` and WinTerm stable-state migration is disabled.
- [x] Packaged state is isolated by the unique package identity.
- [x] Static profile foundations retain PowerShell 7 preference, Windows PowerShell, Command Prompt, WSL discovery, and the missing-WSL guard.
- [x] Safe upstream sync dry run resolves the recorded official target without integrating or pushing.
- [ ] Built-file product metadata validation; no build output exists.
- [ ] Relevant compiled upstream tests; PowerShell 7 and build output are unavailable.
- [ ] Full upstream tests; intentionally skipped until a provisioned runner is available.

## Build

- [ ] Debug x64 builds successfully; blocked before build by missing PowerShell 7 and toolchain.
- [ ] Release x64 builds successfully; blocked before build by missing PowerShell 7 and toolchain.
- [ ] Package project builds; blocked before build by missing PowerShell 7 and toolchain.
- [ ] No new compiler errors; compiler was not available.
- [ ] No unexplained new warnings; compiler was not available.

## Installation and coexistence

- [ ] A signed winTerm MSIX can be installed locally.
- [ ] winTerm and Microsoft Windows Terminal can be installed side by side.
- [ ] Installing winTerm does not replace Microsoft Windows Terminal.
- [ ] winTerm appears separately in the Start menu.
- [x] Source manifest uses an independent package and application identity.
- [x] Source manifest does not claim `wt.exe`.
- [ ] `winterm.exe` launches the installed package.
- [ ] Uninstalling winTerm leaves Microsoft Windows Terminal installed and working.

## Launch and user-visible brand

- [ ] Application launches on Windows 11 x64.
- [ ] No immediate crash occurs.
- [ ] Window title fallback displays `winTerm`.
- [ ] Start menu, package UI, Settings title, About UI, and command-line help display the intended brand.
- [ ] Settings UI opens.
- [ ] Command palette opens.

## Profiles

- [x] Static: PowerShell 7 dynamic discovery and preferred first-launch selection remain present.
- [x] Static: Windows PowerShell profile remains present.
- [x] Static: Command Prompt profile remains present.
- [x] Static: WSL distribution discovery and missing-WSL guard remain present.
- [ ] Runtime: PowerShell 7 profile appears as `PowerShell` and launches when installed.
- [ ] Runtime: Windows PowerShell appears and launches.
- [ ] Runtime: Command Prompt appears and launches `cmd.exe`.
- [ ] Runtime: installed WSL distributions appear and launch.
- [ ] Runtime: a machine without WSL launches winTerm without a WSL-related failure.

## Tabs, panes, windows, and interaction

- [ ] Open new tab.
- [ ] Close tab.
- [ ] Reorder tabs.
- [ ] Open new window.
- [ ] Split horizontally.
- [ ] Split vertically.
- [ ] Resize pane.
- [ ] Focus pane.
- [ ] Close pane.
- [ ] Zoom and restore pane.
- [ ] Keyboard shortcuts operate.
- [ ] Clipboard copy and paste operate.
- [ ] Text selection operates.
- [ ] Search operates.
- [ ] Scrollback operates.

These behaviors are inherited unchanged from upstream but must still be exercised after packaging and branding changes.

## Rendering and input smoke test

Display this test fixture in PowerShell, Command Prompt, and one available WSL profile:

```text
ASCII
中文測試
日本語テスト
한국어 테스트
😀 🚀 ❤️ 👨‍👩‍👧‍👦
┌─┬─┐
│ │ │
└─┴─┘
   
```

Generate indexed and True Color samples in PowerShell:

```powershell
$esc = [char]27
0..15 | ForEach-Object { Write-Host -NoNewline "$esc[48;5;$($_)m  " }
Write-Host "$esc[0m"
0..255 | ForEach-Object { Write-Host -NoNewline "$esc[48;5;$($_)m " }
Write-Host "$esc[0m"
Write-Host "$esc[38;2;255;96;48m24-bit True Color$esc[0m"
```

- [ ] No crash during the rendering test.
- [ ] ASCII and Unicode text have no obvious cell corruption.
- [ ] Chinese IME input works.
- [ ] Japanese and Korean samples display correctly with suitable fonts.
- [ ] Emoji and the family ZWJ sequence are visible.
- [ ] Box drawing aligns under a suitable monospace font.
- [ ] Powerline glyphs align when a matching font is installed.
- [ ] ANSI 16-color output remains functional.
- [ ] ANSI 256-color output remains functional.
- [ ] 24-bit True Color output remains functional.

## Data isolation

- [x] Source: packaged local state is separated by `HelloThisWorld.winTerm`.
- [x] Source: unpackaged local state is `%LOCALAPPDATA%\winTerm`.
- [x] Source: WinTerm does not use the upstream Stable migration path.
- [x] Source: no dedicated log, persistent profile cache, or migration tool was added.
- [ ] First launch does not change an existing Windows Terminal `settings.json` or state file.
- [ ] winTerm settings changes do not alter Windows Terminal settings.
- [ ] winTerm uninstall does not remove Windows Terminal settings or installation.
- [ ] Shared fragment discovery works as a read-only compatibility input.

## CI and maintenance

- [x] Hosted Windows workflow defines source-level Smoke, branding, manifest, assets, settings, and profile checks.
- [x] Manual self-hosted workflow defines x64 Debug and Release builds plus Relevant tests.
- [x] Manual self-hosted workflow defines an unsigned x64 Release package and artifact upload.
- [x] CI contains no signing certificate, publisher secret, Store deployment, or release overwrite.
- [ ] GitHub-hosted workflow execution observed; workflow has not run from this local checkout.
- [ ] Self-hosted full workflow execution observed; a provisioned runner is not available here.

## Final acceptance gate

winTerm v0.1 is complete only when every unchecked build, installation, launch, profile, interaction, rendering, isolation, and applicable CI item above has a recorded pass on the supported Windows 11 x64 target. Until then, the repository is a source-complete foundation implementation with an open release acceptance gate.
