# winTerm v0.2 acceptance

## Verdict

Last updated: 2026-07-14.

**Not accepted.** The current work provides source and asset foundations for Appearance, including source-level Settings extensions, but no Debug or Release build, package, launch, running Settings UI integration, or manual rendering pass has been recorded. A source inspection or successful asset script is not treated as a runtime pass.

`[x]` means the exact check was executed or directly established in this checkout. `[ ]` means failed, blocked, skipped, not integrated, or not tested.

## Recorded baseline

| Item | Result |
| --- | --- |
| Current branch | `main` |
| Current commit before v0.2 working-tree changes | `747d6b06a803b482b8d4fb232efc4a6a772f98b1` |
| v0.1 tag | None observed at the baseline |
| v0.1 baseline | `main@747d6b06a803b482b8d4fb232efc4a6a772f98b1` |
| Microsoft Terminal upstream source | `release-1.25@1cea42d433253d95c4487a3037db48197b5e72f4` |
| Git remotes | `origin` exists; `upstream` is absent |
| Working tree | Understood, non-clean collaborative v0.2 work; existing changes were preserved |
| Existing v0.1 result | Source Smoke was recorded in `docs/v0.1-acceptance.md`; build, package, launch, and manual runtime checks remained open |

- [x] v0.1 acceptance document exists.
- [x] Repository working tree was inspected and treated as user/collaborator work.
- [ ] `upstream` remote exists.
- [ ] A v0.1 release tag exists.
- [ ] Debug x64 build is recorded.
- [ ] Release x64 build is recorded.
- [ ] winTerm launch is recorded.
- [ ] PowerShell, CMD, and WSL profiles are runtime-verified.
- [ ] Tabs, panes, multi-window behavior, and Settings are runtime-verified.

The earlier v0.1 document says `origin` and `upstream` were configured at that time. The current checkout does not have an `upstream` remote, so v0.2 acceptance leaves this gate open.

## Source and asset gates

- [x] Every PowerShell file in `scripts/winterm` parses in Windows PowerShell 5.1.
- [x] `scripts/winterm/validate-assets.ps1` passes for 15 themes and 4 fonts in Windows PowerShell 5.1.
- [x] `scripts/winterm/generate-third-party-notices.ps1 -Check` passes.
- [x] `scripts/winterm/test.ps1 -Suite Smoke` passes in Windows PowerShell 5.1.
- [x] `scripts/winterm/test-rendering.ps1` completes and emits its fixtures; visual rendering remains untested.
- [x] All 48 Appearance C++ source/header files are registered in the Settings Model static-library project by structural project inspection.
- [ ] Appearance C++ compiles and links.
- [x] Appearance, importer, font, and licensing unit-test sources are registered in the Settings Model unit-test project.
- [ ] Appearance, importer, font, and licensing unit tests execute.
- [x] The winTerm package resource list includes the theme/font manifests, Theme files, fonts, licenses, notices, and samples by structural project inspection.
- [ ] A built package passes the appearance payload and package SHA-256 checks.

These boxes must be updated only from actual command results. A manifest pass does not close the compilation boxes.

Recorded commands on 2026-07-14:

| Command | Result |
| --- | --- |
| `powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -File .\scripts\winterm\validate-assets.ps1` | Passed: 15 themes and 4 fonts |
| `powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -File .\scripts\winterm\generate-third-party-notices.ps1 -Check` | Passed |
| `powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -File .\scripts\winterm\test.ps1 -Suite Smoke` | Passed; compiled tests skipped by suite design |
| `powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -File .\scripts\winterm\test-rendering.ps1` | Fixture emission passed; visual result not tested |

## Theme system

- [x] Four original winTerm theme JSON files exist.
- [ ] Theme Registry loads built-in themes in a built application.
- [ ] Licensed bundled themes load in a built application.
- [ ] Protected and imported ID collision behavior passes compiled tests.
- [ ] Invalid themes are rejected without blocking application startup.
- [ ] Global theme selection works.
- [ ] Profile-specific theme selection works.
- [ ] Theme Gallery is accessible and keyboard navigable.
- [ ] Temporary preview can be cancelled and restores the previous appearance.
- [ ] Preview can be committed without restarting a shell.
- [ ] Theme export and re-import preserve supported fields.
- [ ] Migration from a v0.1 scheme is idempotent and preserves unknown settings.

`cursorTextColor` and `selectionForeground` are metadata-only in the current scheme projection. They are not counted as renderer features.

## Import and export

- [ ] XML `.itermcolors` import passes compiled tests.
- [ ] Common XML `.terminal` colors import passes compiled tests.
- [ ] Executable macOS profile fields are ignored and never executed.
- [ ] A single Windows Terminal color scheme imports.
- [ ] A Windows Terminal `schemes` array imports within the count limit.
- [ ] winTerm schema version 1 imports and exports.
- [ ] Malformed, oversized, deeply nested, and over-count inputs are rejected safely.
- [ ] Duplicate JSON keys are rejected.
- [ ] Custom DTD and XML entity expansion are rejected.
- [ ] Errors shown to users are understandable English without stack traces.

Binary outer plist input is intentionally unsupported in v0.2 source and must produce a clear error. XML plist files may contain recognized archived color data; that does not add binary outer plist support.

## Fonts

- [x] Cascadia Code and Cascadia Mono files are represented by pinned manifest entries.
- [x] Cascadia Mono NF is not bundled.
- [ ] The app-private font collection compiles and registers fonts for the winTerm process.
- [ ] Font licenses are included in the package and license page.
- [ ] System fonts can be enumerated without a crash.
- [ ] Recommended families are visible in Settings.
- [ ] A selected app-private font can be applied.
- [ ] A selected system font can be applied.
- [ ] A missing selected font safely falls back after restart.
- [ ] Ligature controls preserve cursor, selection, copy, and cell alignment.
- [ ] Powerline and Nerd Font glyphs work with a compatible user-installed font.

The manifest is an app-private loading plan, not evidence that the fonts were loaded. winTerm must never globally install these files.

## ANSI, emoji, and Unicode

- [ ] ANSI 16 and bright colors render.
- [ ] ANSI 256 cube and grayscale ramp render.
- [ ] 24-bit foreground and background render.
- [ ] Bold, dim, reverse, cursor, and selection interactions pass.
- [ ] Box drawing aligns under a suitable font.
- [ ] Block elements and Braille render without adjacent-cell corruption.
- [ ] Powerline renders under a compatible font.
- [ ] Nerd Font private-use probes render under a compatible installed font.
- [ ] CJK mixed content preserves cell alignment and copy text.
- [ ] Basic emoji renders.
- [ ] Color emoji renders where supported.
- [ ] Skin-tone, ZWJ, flag, variation-selector, and keycap sequences do not crash.
- [ ] Cursor, backspace, selection, and copy preserve tested Unicode sequences.

No v0.2 runtime result exists yet. Upstream renderer reuse is not marked as a pass. Exact ZWJ and color-glyph limitations remain to be measured.

## Licensing

- [x] Every third-party theme passes manifest, checksum, source, and license validation.
- [x] Every app-private font passes manifest, checksum, source, and license validation.
- [x] `THIRD_PARTY_NOTICES.md` matches deterministic local generation.
- [ ] The package contains required appearance licenses and notices.
- [ ] Settings exposes an accessible open-source license page.
- [x] No theme or font is downloaded during normal validation or build.
- [x] No Apple font or Cascadia Mono NF asset is included by the v0.2 manifests.

## Build, package, and stability

- [ ] Debug x64 builds.
- [ ] Release x64 builds.
- [ ] Unsigned development package builds.
- [ ] Package version and identity are verified for the v0.2 development release.
- [ ] winTerm launches without an immediate crash.
- [ ] PowerShell works.
- [ ] Command Prompt works.
- [ ] WSL discovery remains functional, including the missing-WSL case.
- [ ] Tabs, panes, windows, clipboard, selection, search, and scrollback remain functional.
- [ ] Settings remains usable with keyboard, screen reader, high contrast, and display scaling.
- [ ] winTerm and Microsoft Windows Terminal install side by side.
- [ ] A corrupt theme or missing font does not block launch.
- [ ] Startup, gallery, picker, switching, scrollback, emoji-heavy, and True Color performance are measured.

## Scope guard

- [x] No Linux command translation, shell bridge, Clink, completion, AI command generation, online marketplace, or automatic asset download is introduced by the appearance asset and documentation work.
- [x] No renderer core, VT parser, text buffer, Unicode width engine, ConPTY, input parser, or OpenConsole runtime internals are modified by the appearance source boundary. The shared OpenConsole PowerShell helper changes only make build and test failures propagate.
- [x] Final diff review confirms all v0.2 work remains within the approved scope; `git diff --check` and protected-path inspection passed on 2026-07-14.

## Required release record

Before acceptance, attach actual commands and outcomes for source validation, unit tests, integration tests, Debug and Release builds, packaging, install and coexistence, and manual rendering in PowerShell, Command Prompt, and WSL. Record the test device, Windows build, GPU, scaling, selected fonts, package path, architecture, version, signing status, and every known limitation. Do not replace an unchecked item with an inferred pass.
